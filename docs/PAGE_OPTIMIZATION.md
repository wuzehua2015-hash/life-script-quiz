# 人生剧本测试系统 v2.4 - 页面优化清单

## 📋 优化任务总览

### 已创建的设计系统文件
- ✅ `docs/UI_SYSTEM.md` - UI设计系统文档
- ✅ `css/variables.css` - CSS变量系统
- ✅ `css/components.css` - 统一组件库

---

## 🔍 现有页面分析

### 1. 首页 (index.html)
**路径**: `/code/index.html`  
**状态**: ⚠️ 部分优化，需统一引入设计系统

**当前问题**:
- [ ] 内联样式过多，需提取到CSS类
- [ ] 按钮样式不统一（部分使用内联style）
- [ ] 缺少设计系统变量引用
- [ ] 响应式断点不一致

**优化建议**:
```html
<!-- 替换前 -->
<a href="profile/index.html" class="btn-secondary" style="display:inline-flex;...">

<!-- 替换后 -->
<a href="profile/index.html" class="btn btn-secondary">
```

---

### 2. 结果页 (report.html)
**路径**: `/code/report.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 与首页样式不一致
- [ ] 缺少胶片颗粒效果
- [ ] 按钮样式不统一
- [ ] 卡片间距不一致

**优化建议**:
1. 引入 `variables.css` 和 `components.css`
2. 统一使用 `.card` 类替换自定义样式
3. 使用 `.btn-primary` / `.btn-secondary` 替换现有按钮

---

### 3. 个人中心 (profile/index.html)
**路径**: `/code/profile/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `profile.css`
- [ ] 部分样式与设计系统不一致
- [ ] 统计卡片样式可统一使用 `.card` 组件
- [ ] 缺少金色光晕效果

**优化建议**:
```css
/* 替换前 */
.profile-section {
    background: var(--bg-card);
    border-radius: 16px;
    margin: 1rem 1.5rem;
    padding: 1.5rem;
    border: 1px solid var(--film-border);
}

/* 替换后 */
.profile-section {
    composes: card;
    margin: var(--space-4) 0;
}
```

---

### 4. 角色收集 (collection/index.html)
**路径**: `/code/collection/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 依赖 `role-collection.css`
- [ ] 卡片悬停效果不一致
- [ ] 进度条样式可统一

**优化建议**:
1. 使用 `.progress` 组件替换自定义进度条
2. 使用 `.card-interactive` 替换角色卡片
3. 统一使用设计系统的动画变量

---

### 5. 情绪打卡 (mood/index.html)
**路径**: `/code/mood/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `mood.css`
- [ ] 情绪选择器样式可组件化
- [ ] 图表区域缺少统一容器样式

**优化建议**:
```css
/* 创建情绪选择器组件 */
.mood-selector {
    composes: card;
    padding: var(--space-6);
}

.mood-option {
    composes: card-interactive;
    /* 自定义情绪选项样式 */
}
```

---

### 6. 日记系统 (diary/index.html)
**路径**: `/code/diary/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `diary.css`
- [ ] 日历样式与整体风格不一致
- [ ] 编辑器样式可统一

**优化建议**:
1. 使用 `.input` 和 `.textarea` 组件
2. 日历使用设计系统颜色变量
3. 标签使用 `.tag` 组件

---

### 7. 应对指南 (guide/index.html)
**路径**: `/code/guide/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `guide.css`
- [ ] 场景卡片样式可统一
- [ ] 步骤指示器样式不一致

**优化建议**:
```css
/* 场景卡片 */
.scenario-card {
    composes: card-interactive;
    border-left: 4px solid var(--scenario-color);
}

/* 步骤卡片 */
.step-card {
    composes: card;
    border-left: 3px solid var(--step-color);
}
```

---

### 8. 行动指导 (guidance/index.html)
**路径**: `/code/guidance/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `guidance.css`
- [ ] 头部样式与其他页面不一致
- [ ] 原型卡片样式可统一

**优化建议**:
1. 使用设计系统的头部样式
2. 原型卡片使用 `.card-interactive`
3. 统一使用 `.btn-primary` 样式

---

### 9. 紧急支持 (emergency/index.html)
**路径**: `/code/emergency/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `emergency.css`
- [ ] 呼吸练习动画样式可优化
- [ ] 工具卡片样式可统一

**优化建议**:
```css
/* 工具卡片 */
.tool-card {
    composes: card-interactive;
    text-align: center;
    padding: var(--space-6);
}

/* 呼吸动画容器 */
.breathing-visual {
    composes: card-highlight;
    aspect-ratio: 1;
    display: flex;
    align-items: center;
    justify-content: center;
}
```

---

### 10. 资源库 (resources/index.html)
**路径**: `/code/resources/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `resources.css`
- [ ] 资源卡片样式可统一
- [ ] 筛选器样式不一致

**优化建议**:
1. 资源卡片使用 `.card-interactive`
2. 筛选按钮使用 `.btn-ghost` 变体
3. 标签使用 `.tag` 组件

---

### 11. 成就系统 (achievements/index.html)
**路径**: `/code/achievements/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `badges.css`
- [ ] 徽章卡片样式可统一
- [ ] 统计区域样式不一致

**优化建议**:
```css
/* 徽章卡片 */
.badge-card {
    composes: card;
    display: flex;
    align-items: center;
    gap: var(--space-4);
}

.badge-card.unlocked {
    composes: card-highlight;
}
```

---

### 12. 改变轨迹 (journey/index.html)
**路径**: `/code/journey/index.html`  
**状态**: ⚠️ 需优化

**当前问题**:
- [ ] 使用独立CSS文件 `journey.css`
- [ ] 颜色方案与其他页面不一致（使用紫色渐变）
- [ ] 时间轴样式可优化

**优化建议**:
1. 统一使用金色主题
2. 时间轴使用设计系统颜色
3. 统计卡片使用 `.card` 组件

---

## 📊 CSS文件分析

| 文件 | 大小 | 状态 | 建议 |
|------|------|------|------|
| style.css | ~15KB | ✅ 基础良好 | 整合变量系统 |
| profile.css | ~8KB | ⚠️ 需优化 | 合并到组件库 |
| journey.css | ~7KB | ⚠️ 需优化 | 统一主题色 |
| diary.css | ~6KB | ⚠️ 需优化 | 使用表单组件 |
| emergency.css | ~10KB | ⚠️ 需优化 | 提取通用组件 |
| resources.css | ~7KB | ⚠️ 需优化 | 使用卡片组件 |
| mood.css | ~5KB | ⚠️ 需优化 | 使用表单组件 |
| badges.css | ~8KB | ⚠️ 需优化 | 使用卡片组件 |
| role-collection.css | ~6KB | ⚠️ 需优化 | 使用卡片组件 |
| guide.css | ~8KB | ⚠️ 需优化 | 统一按钮样式 |
| guidance.css | ~4KB | ⚠️ 需优化 | 统一头部样式 |

---

## 🎯 优化优先级

### 🔴 高优先级
1. **index.html** - 首页入口，影响第一印象
2. **profile/index.html** - 用户高频访问页面
3. **emergency/index.html** - 紧急页面需要稳定体验

### 🟡 中优先级
4. **report.html** - 测试结果页
5. **collection/index.html** - 角色收集
6. **achievements/index.html** - 成就系统
7. **guide/index.html** - 应对指南
8. **guidance/index.html** - 行动指导

### 🟢 低优先级
9. **mood/index.html** - 情绪打卡
10. **diary/index.html** - 日记系统
11. **resources/index.html** - 资源库
12. **journey/index.html** - 改变轨迹

---

## 🔧 具体优化步骤

### 第一步：引入设计系统
在每个HTML文件的 `<head>` 中添加：
```html
<!-- 1. 基础变量系统 -->
<link rel="stylesheet" href="../css/variables.css">

<!-- 2. 组件库 -->
<link rel="stylesheet" href="../css/components.css">

<!-- 3. 页面特定样式（最后加载） -->
<link rel="stylesheet" href="page-specific.css">
```

### 第二步：替换按钮样式
```html
<!-- 替换前 -->
<button class="custom-btn" style="...">点击</button>

<!-- 替换后 -->
<button class="btn btn-primary">点击</button>
```

### 第三步：替换卡片样式
```html
<!-- 替换前 -->
<div class="custom-card">内容</div>

<!-- 替换后 -->
<div class="card">内容</div>
```

### 第四步：统一颜色引用
```css
/* 替换前 */
color: #d4af37;

/* 替换后 */
color: var(--color-gold);
```

---

## 📈 预期效果

### 优化前
- 11个独立CSS文件
- 多处内联样式
- 颜色/间距不一致
- 维护困难

### 优化后
- 2个核心CSS文件（variables + components）
- 少量页面特定样式
- 统一的视觉语言
- 易于维护和扩展

---

## ✅ 验收标准

- [ ] 所有页面引入 `variables.css` 和 `components.css`
- [ ] 无内联样式（除动态计算外）
- [ ] 按钮样式统一使用 `.btn` 类
- [ ] 卡片样式统一使用 `.card` 类
- [ ] 颜色统一使用CSS变量
- [ ] 间距统一使用CSS变量
- [ ] 动画统一使用CSS变量
- [ ] 响应式表现一致

---

*文档版本: v2.4*  
*最后更新: 2026-02-28*
